language: generic
sudo: required
services: 
    - docker

before_install:
    - docker build -t jisacson/docker-react -f Dockerfile.dev .

script: 
    - docker run -e CI=true jisacson/docker-react npm run test